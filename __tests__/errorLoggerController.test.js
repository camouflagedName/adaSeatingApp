import { expect } from "chai";
import { logError } from "../controllers/errorLoggerController";


describe('Error logging', () => {
    it('should log errors to the file', async () => {
        
    })
})